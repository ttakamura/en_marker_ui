import assert from 'assert';
import { describe, before, it } from 'mocha';
import { predict } from '../../app/api';

describe('predict API', () => {
  it('should call the API', () => {

  });

  it('should return result as Sentence and Token', () => {

  });
});
